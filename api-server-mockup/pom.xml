<?xml version="1.0" encoding="UTF-8"?>
<!-- 
REMINDER:

(1) To inject variables from pom.xml into resources files:
(1.1) In pom.xml, plugins section, filtering should be enabled.
(1.2) Delimiter '@' is defined by SpringBoot2 maven plugin.
(1.3) Without it, delimiter is '${}' .

(2) JSP has limited support:
(2.1) JSP files are always read from "src/main/webapp/WEB-INF/jsp/" folder.
(2.2) Project packaging should be WAR.  Project can be standalone-run or server-deployed.
(2.3) Application properties "spring.mvc.view.prefix=/WEB-INF/jsp/" and "spring.mvc.view.suffix=.jsp" should be enabled.
(2.4) If project is packaged as JAR, JSPs are not read (see SpringBoot2 documentation, JSP limitaions).

-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	
	<modelVersion>4.0.0</modelVersion>
	
 
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>2.6.3</version>
	</parent>
	
	<groupId>localhost</groupId>
	<artifactId>api-server-mockup</artifactId>
	<version>1.0.0</version>
	<name>api-server-mockup</name>
	<description>SpringBoot2 small API server for fast testing purposes</description>
	<packaging>jar</packaging>

	<properties>
		<java.version>1.8</java.version>
		<project.versionDate>2022-02-22 13:00</project.versionDate>
		<project.versionComment>mockup para bm, hash sha-384 in http header, hash calculation and validation</project.versionComment>
		<maven.test.skip>false</maven.test.skip>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		    <exclusions>
		    	<!-- logging -->
		        <exclusion>
		            <groupId>org.springframework.boot</groupId>
		            <artifactId>spring-boot-starter-logging</artifactId>
		        </exclusion>
				<!-- Jackson -->
				<!-- 
				<exclusion>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-starter-json</artifactId>
				</exclusion>
				-->
		    </exclusions>
		</dependency>
		
		<dependency>
		    <groupId>org.springframework.boot</groupId>
		    <artifactId>spring-boot-starter-log4j2</artifactId>
		</dependency>

		<!-- -->
		<dependency>
		    <groupId>org.springframework.boot</groupId>
		    <artifactId>spring-boot-starter-security</artifactId>
		</dependency>

		<!-- -->
		<dependency>
		    <groupId>org.springframework.boot</groupId>
		    <artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>

        <!-- dependency for jsp, use only with WAR packaging (see REMINDER) -->
        <!--   
		<dependency>
    		<groupId>javax.servlet</groupId>
    		<artifactId>jstl</artifactId>
		</dependency>
		-->
		
		
		<!-- dependency for jsp, use only with WAR packaging (see REMINDER) -->
        <!--   
		<dependency>
		    <groupId>org.apache.tomcat.embed</groupId>
    		<artifactId>tomcat-embed-jasper</artifactId>
    		<scope>provided</scope>
		</dependency>
		-->
		
		
		<dependency>
		    <groupId>org.springframework.boot</groupId>
		    <artifactId>spring-boot-starter-test</artifactId>
		    <scope>test</scope>
		</dependency>
        
        
		<!-- uncomment only when debugging -->
		<!-- --> 
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<!-- <optional>true</optional> -->
		</dependency>
		
       
        <!-- local testing -->
		<dependency>
		    <groupId>com.h2database</groupId>
		    <artifactId>h2</artifactId>
		    <!-- <scope>test</scope> -->
            <scope>runtime</scope>
		</dependency>
        

		<!-- -->
		<dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
        </dependency>
        
	</dependencies>

	<build>
		<finalName>api-server-mockup</finalName>
		
		<plugins>

			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>

		</plugins>
		
		<resources>

		    <resource>
		        <filtering>true</filtering>
		        <directory>src/main/resources</directory>
		        <!-- 
		        <includes>
		            <include>application.properties</include>
		        </includes>
		        -->
		    </resource>

		</resources>
				
	</build>

</project>
